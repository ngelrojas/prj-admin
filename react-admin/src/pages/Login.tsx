import React, {useState} from "react";

const Login = () => {
    const [count, setCount] = useState(0);

    return (
        <div>
            <h1>Login</h1>
        </div>
    )
}

export default Login;